const name="Hitesh"
const repoCount=50

// console.log(name + repoCount);

//string interpolation is better to use
//String interpolation refers to the process of embedding variables or expressions within a string literal, allowing for dynamic content generation using backticks (`) and placeholders (${expression}).

console.log(`Hello my name is ${name} and my repo count is ${repoCount}`);
console.log(`${name} and ${repoCount}`);

